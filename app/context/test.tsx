// this directory hold the context like auth context if we are using context else use store with zustand
import React from 'react';

type Props = {};

function test({}: Props) {
  return <div>test</div>;
}

export default test;
