// components: this directory hold the common UI component like card button and all
import React from 'react';

type Props = {};

function test({}: Props) {
  return <div>test</div>;
}

export default test;
